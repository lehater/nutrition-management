# Baseline and provenance

This directory is for explicit historical project snapshots or provenance packets whose later comparison/audit/migration value justifies keeping them in the working tree.

It is not a second source of current truth. Living project truth remains in `requirements/`, `domain/`, `architecture/` and accepted ADRs.

Git history is the default archive; create a baseline only deliberately.
